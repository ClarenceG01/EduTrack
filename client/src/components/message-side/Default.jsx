import React from "react";

const Default = () => {
  return (
    <h1
      style={{
        margin: "15rem auto",
        fontWeight: "500",
      }}
    >
      Click user start conversation
    </h1>
  );
};

export default Default;
